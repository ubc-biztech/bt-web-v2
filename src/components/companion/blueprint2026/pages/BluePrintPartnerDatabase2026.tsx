import BluePrintLayout from "../layout/BluePrintLayout";
import { DynamicPageProps } from "@/constants/companion-events";

export default function BluePrintPartnerDatabase2026(_props: DynamicPageProps) {
  return <BluePrintLayout>Partner database page</BluePrintLayout>;
}
